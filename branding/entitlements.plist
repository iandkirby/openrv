<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the notarized Sequence RV (OpenRV) app.

  Notarization forces the hardened runtime, which by default blocks the very
  things RV needs to run:
    * disable-library-validation  — RV loads its own unsigned plugins, Qt
      plugins, OCIO, FFmpeg and dozens of bundled dylibs not signed by our
      Team ID; without this the runtime refuses to load them and RV exits.
    * allow-unsigned-executable-memory / allow-jit / disable-executable-page-
      protection — the embedded CPython (and any JITs it pulls in) allocate
      writable+executable memory, which hardened runtime forbids by default.
    * audio-input / camera — RV can read A/V devices; harmless if unused.
  Keep this list minimal but sufficient: trimming library-validation or the
  JIT entitlements is the usual cause of a notarized-but-crashes-on-launch app.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.cs.disable-executable-page-protection</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.device.camera</key>
    <true/>
</dict>
</plist>
